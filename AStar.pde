/*class AStar {
  ArrayList<Tile> open;
  ArrayList<Tile> closed;
  Tile[][] board;
  
  Tile start;
  Tile end;
  
  AStar(Tile start, Tile end, Tile[][] board){
    this.start = start;
    this.end = end;
    this.board = board;
  }
  
  int findDirection(Tile start, Tile end) {
    Tile tmp = start;
    start.
    while(!tmp.equals(end)){
      open.add(tmp);
      open.add(Tile[tmp.getYcor()+1][tmp.getXcor()]);
      start.getXcor()
    
  
